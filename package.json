{
  "name": "discordbotcam",
  "version": "1.0.0",
  "description": "Discord operations agent with safe Solana token validation and validator console lease intake.",
  "main": "index.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "check": "node --check index.js && node --check bot.js && node --check canvasy.js && node --check src/agent.js && node --check src/lease.js && node --check src/solana.js && node --check scripts/agent-smoke.js && node --check scripts/agent-live-smoke.js",
    "unit": "node --test",
    "agent:smoke": "node scripts/agent-smoke.js",
    "agent:smoke:live": "node --env-file-if-exists=.env scripts/agent-live-smoke.js",
    "devcontainer:check": "node -e \"const fs = require('node:fs'); const config = JSON.parse(fs.readFileSync('.devcontainer/devcontainer.json', 'utf8')); if (config.build?.dockerfile !== 'Dockerfile') throw new Error('devcontainer Dockerfile is not configured'); console.log('devcontainer config valid');\"",
    "canvas:smoke": "node -e \"const { createCanvas } = require('canvas'); const canvas = createCanvas(2, 2); const png = canvas.toBuffer('image/png'); if (!Buffer.isBuffer(png) || png.length === 0) throw new Error('canvas smoke test failed'); console.log('canvas smoke test passed:', png.length, 'bytes');\"",
    "test": "npm run check && npm run unit && node scripts/agent-smoke.js && npm run devcontainer:check && npm run canvas:smoke"
  },
  "keywords": [
    "discord",
    "bot",
    "openai-agents",
    "solana",
    "web3",
    "gas-status"
  ],
  "author": "dex",
  "license": "ISC",
  "dependencies": {
    "@openai/agents": "^0.17.0",
    "canvas": "3.2.3",
    "discord.js": "^14.16.3",
    "zod": "^4.4.3"
  }
}
